type Author {
  id: Int!
  firstName: String
  lastName: String
  posts: [Post!]!
}

type Post {
  id: Int!
  title: String!
  authorId: Float!
  votes: Int
}

type Query {
  author(id: Int!): Author!
  authors: [Author!]!
}
